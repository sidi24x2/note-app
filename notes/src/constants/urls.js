const rootURL = '/api';
const loginURL = rootURL + '/users/login';
const registerURL = rootURL + '/users/register';
const notesURL = rootURL + '/notes';
const favoritesURL = rootURL + '/favorites';

export { rootURL, loginURL, registerURL, notesURL, favoritesURL };
